<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="main.css">
    <script src="bundle.js" async></script>
  </head>
  <body>

    <!-- 
    data-controller 连接 hello_controller
    data-target 指定属性 会默认生成nameTarget
    data-action的值click->hello#greet称为action描述符
    click 是事件名称
    hello 是控制器标识符
    greet 是要调用的方法名称

    常用默认事件可以省略
      a -> click 
      button -> click 
      form -> submit 
      input -> change 
      input type=submit -> click 
      select -> change 
      textarea -> change
    -->
    <div data-controller="hello">
      <input data-target="hello.name" type="text">
      <button data-action="click->hello#greet">Greet</button>
    </div>

    <div data-controller="clipboard">
      PIN: <input type="text" value="1235" data-target="clipboard.source" readonly>
      <button data-action="clipboard#copy" class="clipboard-button">Copy to Clipboard</button>
    </div>


    <div data-controller="clipboard">
      PIN: <input data-target="clipboard.source" type="text" value="3737" readonly>
      <button data-action="clipboard#copy" class="clipboard-button">Copy to Clipboard</button>
  </div>


  <div>

    <button data-action="slideshow#previous"><- </button>
    <button data-action="slideshow#next"> <- </button>
    <div data-target="slideshow.slide"class="slide">🐵</div>
    <div data-target="slideshow.slide"class="slide">🐵</div>
    <div data-target="slideshow.slide"class="slide">🐵</div>
    <div data-target="slideshow.slide"class="slide">🐵</div>
    <div data-target="slideshow.slide"class="slide">🐵</div>
    </div>
  </body>
</html>
